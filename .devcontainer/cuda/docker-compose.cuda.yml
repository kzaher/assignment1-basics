services:
  cs336-dev:
    build:
      dockerfile: cuda/Dockerfile.cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]